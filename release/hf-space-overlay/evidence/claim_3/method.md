# Method — Claim 3

The linear matrix is diagonal with strictly negative entries. The nonlinear
operator is coordinatewise `gamma*tanh(x)`; its Euclidean Lipschitz constant is
`max(gamma)<1`, and its second derivative is globally bounded. Both use the
same d=8 skew bounded iid noise, five alpha values, four seeds, W1 brackets,
held-out envelopes, and wrong-rate controls.
